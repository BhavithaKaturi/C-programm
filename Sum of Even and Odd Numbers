#include <stdio.h>
int main() {
    int n, sumEven=0, sumOdd=0, i;
    printf("Enter N: ");
    scanf("%d",&n);
    for(i=1;i<=n;i++){
        if(i%2==0) sumEven+=i;
        else sumOdd+=i;
    }
    printf("Sum Even: %d, Sum Odd: %d\n", sumEven, sumOdd);
    return 0;
}
