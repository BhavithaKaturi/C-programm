#include <stdio.h>
int main() {
    int a,b, tempA,tempB;
    printf("Enter two numbers: ");
    scanf("%d %d",&a,&b);
    tempA=a; tempB=b;
    while(tempB!=0){
        int t = tempB;
        tempB = tempA%tempB;
        tempA = t;
    }
    printf("GCD: %d\n",tempA);
    return 0;
}
